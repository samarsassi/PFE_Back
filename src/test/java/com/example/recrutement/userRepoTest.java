// package com.example.recrutement;

/*import com.example.recrutement.entities.User;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

import static org.junit.jupiter.api.Assertions.assertEquals;

@SpringBootTest
public class userRepoTest {
    @Test
    void testUserConstructor() {
        User testUser = new User("John ", 25);
        assertEquals("John ", testUser.getFirstName());
        assertEquals(25, testUser.getAge());
    }

    @Test
    void testSettersAndGetters() {
        User testUser = new User();
        testUser.setId(1L);
        testUser.setFirstName("Alice");
        testUser.setAge(30);

        assertEquals(1, testUser.getId());
        assertEquals("Alice", testUser.getFirstName());
        assertEquals(30, testUser.getAge());
    }

    @Test
    void testToString() {
        User testUser = new User("Bob", 40);
        String expected = "User{, name='Bob', Age=40}";
        assertEquals(expected, testUser.toString());
    }
}*/
