apiVersion: v1
kind: Service
metadata:
  name: flowable-service
  namespace: default
  labels:
    app: flowable
spec:
  selector:
    app: flowable
  ports:
  - name: http
    protocol: TCP
    port: 8080
    targetPort: 8080
  type: ClusterIP
