apiVersion: v1
kind: Service
metadata:
  name: database-service
  namespace: default
  labels:
    app: database
spec:
  selector:
    app: database
  ports:
  - name: mysql
    protocol: TCP
    port: 3306
    targetPort: 3306
  type: ClusterIP
